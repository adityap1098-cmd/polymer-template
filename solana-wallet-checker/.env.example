# Solana Wallet Checker Configuration

# Solana RPC Endpoint (required for real-time monitoring)
# Get your endpoint from: https://www.quicknode.com/
# Example: https://your-endpoint.solana-mainnet.quiknode.pro/your-api-key/
# Or use any Solana RPC endpoint
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Solana WebSocket Endpoint (for real-time transaction streaming)
# Example: wss://your-endpoint.solana-mainnet.quiknode.pro/your-api-key/
SOLANA_WSS_URL=wss://api.mainnet-beta.solana.com

# Alternative: Use public Solana RPC (limited rate)
# PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com
# PUBLIC_WSS_URL=wss://api.mainnet-beta.solana.com

# Wallet classification thresholds
# Wallets with more than this number of different tokens = OLD wallet
OLD_WALLET_THRESHOLD=5

# Polling interval in seconds (for RPC polling mode)
POLL_INTERVAL=5
